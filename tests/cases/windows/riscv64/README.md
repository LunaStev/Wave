# Windows riscv64 cases

This suite is reserved for the requested OS/architecture layout. Wave does not currently
advertise a Windows riscv64 target, so its Wave compatibility cases are syntax-checked but
remain excluded from execution and artifact generation by `cases.toml`.
