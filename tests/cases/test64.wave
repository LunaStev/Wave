struct Box {
    size: i32;
}

proto Box {
    fun double_size(self: Box) -> i32 {
        return self.size * 2;
    }
}

fun main() {
    var b: Box = Box { size: 21 };
    println("{}", b.double_size());
}
