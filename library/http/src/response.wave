import("types");

fun parse_response(raw_response: string): HttpResponse {
    var response = HttpResponse {
        status_code = 200;
        headers = create_map();
        body = raw_response;
    };
    return response;
}