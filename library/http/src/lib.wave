import("request");
import("response");
import("utils");
import("types");

fun send_get(url: string): HttpResponse {
    return send_get_request(url);
}

fun send_post(url: string, body: string): HttpResponse {
    return send_post_request(url, body);
}