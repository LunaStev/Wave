import("sys");

fun println(msg: str) {
    // Output string to console
    system_print(msg);
}

fun input(prompt: str, out: var i32) {
    // Output prompt message
    println(prompt);
    // Receive input from the user
    out = system_input(); // Functions that import the user's input
}